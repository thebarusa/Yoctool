name: Build and Release Yoctool (Linux)

on:
  push:
    tags:
      - 'v*'
  pull_request:
    branches: [ main ]

permissions:
  contents: write

jobs:
  build-release-linux:
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12' 

      - name: Install utility dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y zip

      - name: Run build.sh
        env:
          # Truyền tên Tag (ví dụ v1.0.0) vào biến môi trường BUILD_VERSION
          BUILD_VERSION: ${{ github.ref_type == 'tag' && github.ref_name || github.sha }}
        run: |
          chmod +x build.sh
          ./build.sh

      - name: Package Release
        if: startsWith(github.ref, 'refs/tags/')
        run: |
          TAG_NAME=${GITHUB_REF#refs/tags/}
          # Tên file mong đợi: Yoctool_v1.0.0
          EXE_NAME="Yoctool_${TAG_NAME}"
          
          cd dist
          # Kiểm tra file có tồn tại không
          if [ -f "$EXE_NAME" ]; then
              zip -r "Yoctool-Linux-${TAG_NAME}.zip" "$EXE_NAME"
              echo "Packaging completed: Yoctool-Linux-${TAG_NAME}.zip"
          else
              echo "Error: File $EXE_NAME not found in dist/"
              ls -la
              exit 1
          fi

      - name: Upload to GitHub Releases
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: dist/*.zip
          body: |
            ## Yoctool Linux Build
            Version: ${{ github.ref_name }}
            
            **Installation:**
            1. Download the zip file.
            2. Extract it.
            3. Run: `sudo ./Yoctool_${{ github.ref_name }}`
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}